{
  "name": "@playground/api",
  "private": true,
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "build": "bun x tsc -p tsconfig.json --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "hono": "^4.0.0",
    "zod": "^3.23.0",
    "@playground/shared": "workspace:*"
  }
}
